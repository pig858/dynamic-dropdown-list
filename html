<select id="areaSelect">
    <option value="no" selected>Please select!</option>
    <?php
        /* do some stuff to show ur first level data */
        $strSQL = "SELECT `area` FROM country WHERE 1 ";
        $areaData = query($strSQL);
        foreach($areaData as $key => $value){
            $class_sn = $value[class_sn];
            echo '<option value="'.$class_sn.'" '.(($area_sn==$class_sn)? "selected":"").'>'.$value[class_name].'</option>';
        }
        /* just a pretty easy simple for this*/
    ?>
</select>
<select id="citySelect">
      <option value="" selected>Please select!</option>
</select>
  <script>areaChange();</script> 
